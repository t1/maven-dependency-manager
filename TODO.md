* add a new command `show-available` that accepts `groupId:artifactId` or `groupId` or `artifactId` and displays all
  available versions
* add an option `--force-cache-update` / `-f` to refresh all maven metadata used for lookup of the latest version
